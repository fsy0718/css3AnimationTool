.Lfll{
  float: left;
}
.Lflr{
  float: right;
}
ul{
  margin: 0;
  padding: 0;
  list-style: inside none;
}
dl{
  margin: 0;
}
dd{
  margin-left:0;
}


.layout{
  box-sizing: border-box;
  padding-top: 38px;
  .pure-u-mid-3-4{
    width: 75%;
  }
  .pure-u-mid-1-4{
    width: 25%;
  }
  .pure-button{
    margin-left: 1.5em;
    .fa{
      margin-right: 0.5em;
    }
  }
  .pure-menu{
    padding: 0.5em 0;
  }
  .button-success,
  .button-error,
  .button-warning,
  .button-secondary {
      color: white;
      border-radius: 4px;
      text-shadow: 0 1px 1px rgba(0, 0, 0, 0.2);
  }

  .button-success {
      background: rgb(28, 184, 65); /* this is a green */
  }

  .button-error {
      background: rgb(202, 60, 60); /* this is a maroon */
  }

  .button-warning {
      background: rgb(223, 117, 20); /* this is an orange */
  }

  .button-secondary {
      background: rgb(66, 184, 221); /* this is a light blue */
  }
}
.button-small{
  font-size: 85%;
}

.header{
  position: fixed;
  top: 0;
  left: 0;
  font-size: 12px;
  box-shadow: 0 2px 5px rgba(0,0,0,0.2);
  background-color: white;
  .predefined{
    margin-left: 1.5em;
  }
  .grid{
    padding-left: 1.5em;
    i{
      font-style: normal;
      padding-left: 0.5em;
    }
    input[type="checkbox"]{
      vertical-align: middle;
    }
  }
}
html,body,.layout{
  width: 100%;
  height: 100%;
}
.canvasbox{
  display: flex;
  justify-content: flex-end;
  width: 100%;
  height: 100%;
  padding-right: 2%;
  box-sizing: border-box;
  align-items: center;
  .canvas{
    box-shadow: 0 0 5px rgba(0,0,0,0.2);
    width: 640px;
    max-height: 800px;
    min-height: 400px;
  }
  .animation_plus_tool_item{
      position:relative;
      .animation_plus_tool_item_origin{
        position: absolute;
        top: 50%;
        left: 50%;
        width: 10px;
        margin-top: -5px;
        margin-left: -5px;
        height: 10px;
        background-color: red;
        border-radius: 50%;
        opacity: .5;
      }
      &.animation_plus_tool_item_active{
          .animation_plus_tool_item_origin{
              background-color: red;
        }
      }
  }
}

.pure-form{
  .pure-control-group{
    margin-bottom: 1em;
  }
}

.canvas{
  [class *= "pure-u"]{
    border: dashed 1px rgba(0,0,0,0.5);
    min-height: 5px;
  }
}

.pure-form{
  [class *= "pure-u"].error{
    color: red;
  }
}

.popup{
  display: flex;
  align-items: center;
  justify-content: center;
  position: fixed;
  top: 0;
  left: 0;
  background-color: rgba(0,0,0,.02);
  width: 100%;
  height: 100%;
  .popupbox{
    display: inline-block;
    padding: 1em;
    box-shadow: 0 0 3px rgba(0,0,0,.4);
    text-align: center;
    background-color: white;
  }
}
.popup-menu{
  display: none;
  .popupbox{
    position: absolute;
    .close{
        font-style: normal;
        position: absolute;
        top: 10px;
        right: 10px;
        cursor: pointer;
    }
  }
  .menulist{
    li{
      cursor: pointer;
      line-height: 1.5;
    }
  }
}

.layer-style{
  .tab-nav{
    line-height: 2;
    text-align: center;
    padding: 10px 30px;
    .tab-nav-item{
      a{
        display: block;
        border: solid 1px rgba(0,0,0,0.5);
        background-color: rgba(0,0,0,0.2);
        color: rgba(0,0,0,0.8);
        text-decoration: none;
        &:hover{
          background-color: rgba(28,184,65,0.4);
          color: white;
        }
      }
      &.active{
        a{
          background-color: rgba(28,184,65,0.4);
          color: white;
        }
      }
      & + .tab-nav-item{
        a{
          border-left: 0 none;
        }

      }
    }
  }
  .tab-cont{
    margin-top: 2em;
    margin-bottom: 1em;
  }
  .tab-cont-item{
    display: none;
    &.active{
      display: block;
    }
  }
  .tab-cont-item-transform{
    font-size: 0.75em;
    input{
      &[type="number"],&[type="text"]{
        width: 3em;
      }
    }
    .list{
      padding-left: 2em;
    }
    .s2d,.s3d{
      padding: 0.25em 0;
    }
  }
  .sort-title{
    padding: 10px 0;
    border-bottom: solid 1px rgba(0,0,0,0.2);
    margin-bottom: 10px;
  }
  .sort-content{
    .pure-button{
      color: rgba(0,0,0,0.6);
      &.button-success{
        background: rgb(28, 184, 65); /* this is a green */
        color: white;
      }
    }
  }
}
